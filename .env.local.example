# MongoDB Atlas connection string (Snobo Labs cluster)
MONGODB_URI=

# Resend API key (for contact/hire form emails) — get from resend.com
RESEND_API_KEY=

# Email addresses
NOTIFY_EMAIL=
# Backend API URL (Express server — see /server folder)
NEXT_PUBLIC_API_URL=
